すべての引数を第一引数の型に強制変換し、次に第二引数の型に強制変換するという試みは、適切ではない。

'(complex scheme-number)の組み合わせにしか関数が定義されていない場合、
この戦略では'(complex complex)に変換されるので関数を適用させることが出来ない。

多くの引数を持つ一般の場合に、強制型変換が使えるようにapply-genericを一般化する方法を示す。
